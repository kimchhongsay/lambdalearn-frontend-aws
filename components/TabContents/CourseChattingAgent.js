import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CourseChattingAgent = () => {
  return (
    <View>
      <Text>CourseChattingAgent</Text>
    </View>
  );
};

export default CourseChattingAgent;

const styles = StyleSheet.create({});
