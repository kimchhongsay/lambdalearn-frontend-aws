import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProgressDashboard = () => {
  return (
    <View>
      <Text>ProgressDashboard</Text>
    </View>
  );
};

export default ProgressDashboard;

const styles = StyleSheet.create({});
