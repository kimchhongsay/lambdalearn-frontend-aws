import { StyleSheet, Text, View } from "react-native";
import React from "react";

const RecordingSummaries = () => {
  return (
    <View>
      <Text>RecordingSummaries</Text>
    </View>
  );
};

export default RecordingSummaries;

const styles = StyleSheet.create({});
